module if_expr
{
    /// This is a check for a condition.
    /// @param condition a boolean to check if the function is valid
    /// @return an integer
    public static func check(condition: bool) -> i32 
    {
        // if expression
        var result = if condition then 1 else 2

        while condition {

        }

        // if statement
        if condition then {
            result++
        } else {
            result++
        }

        return result
    }

    public static func check_inline(condition: bool) -> i32 => if condition then 1 else 2
}
